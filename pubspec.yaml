name: ios_music_player
description: A personal offline music player.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"      # <--- Thêm dòng này

dependencies:
  flutter:
    sdk: flutter

  youtube_explode_dart: ^2.2.0
  path_provider: ^2.1.5
  audioplayers: ^6.1.0
  sqflite: ^2.4.1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true